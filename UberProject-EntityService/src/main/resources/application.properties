
spring.datasource.url=jdbc:mysql://localhost:3306/Uber_Db_Local
# Fix: Added the missing username property
spring.datasource.username=root
spring.datasource.password=sahil#123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate Configuration ---
spring.jpa.show-sql=true
# Note: 'validate' expects Flyway to have already created the tables.
# If tables don't exist yet, change this to 'update' for the first run.
spring.jpa.hibernate.ddl-auto= update
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
logging.level.org.springframework.data=DEBUG
# --- Flyway Configuration ---
spring.flyway.url=jdbc:mysql://localhost:3306/Uber_Db_Local
spring.flyway.user=root
spring.flyway.password=sahil#123
