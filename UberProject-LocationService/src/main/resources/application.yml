spring:
  application:
    name: LocationService

server:
  port: 7777

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka/
  instance:
    prefer-ip-address: true
